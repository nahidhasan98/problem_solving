#include<iostream>
#include<stdio.h>
#include<cstring>
using namespace std;

#define FI freopen( "in.txt", "r", stdin );
#define FO freopen( "out.txt", "w", stdout );

int main() {
    int n, m;
    cin >> n >> m;

    int arr[27];
    memset ( arr, 0, sizeof ( arr ) );
    cout << arr[8] << endl;
    char a, b;
    for ( int i = 0; i < m; i++ ) {
        cin >> a >> b;
        arr [a-92]
    }

    return 0;
}
